[project]
name = ""
version = "0.0.1"
requires-python = "~=3.12"
dependencies = ["davidkhala-databricks[connect]"]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
